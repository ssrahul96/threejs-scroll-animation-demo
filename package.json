{
  "name": "three-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp rahul.jpg dist/ && cp moon.jpg dist/ && cp normal.jpg dist/ && cp space.jpg dist/",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.3.0"
  },
  "dependencies": {
    "three": "^0.137.0"
  }
}
